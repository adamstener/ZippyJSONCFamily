// swift-tools-version:5.3

import PackageDescription

// This package exists to prevent Xcode from displaying the fastlane folder.

let package = Package(name: "fastlane", products: [], targets: [])
